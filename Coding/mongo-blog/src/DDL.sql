use chuwa;

--  create table Posts (
--  id BIGINT NOT NULL AUTO_INCREMENT,
--  title varchar(255) UNIQUE,
--  content varchar(255),
--  description varchar(255),
--  primary key(id)
--  );

select * from Posts;