type Query{
    getById(id: ID): Post
    getAllPost: [Post]
}

type Post{
    id: ID
    title: String
    content: String
    description: String
}
type Mutation{
    createPost(title: String!, content: String!, description: String!): Post
    deleteById(id:ID!): Post
}